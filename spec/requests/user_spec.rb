require 'rails_helper'

RSpec.describe 'User Sessions' do
  # it 'signs user in and out' do
  #   user = create(:user, :valid_user)    ## uncomment if using FactoryBot
  #   # user = User.create(email: 'test@test.com', password: "password", password_confirmation: "password", name: "test name") ## uncomment if not using FactoryBot
  #   sign_in user
  #   post user_session_path
  #   expect(response).to have_http_status(200)
  #   #expect(response).to render_template(:index) # add gem 'rails-controller-testing' to your Gemfile first.
  #
  #   sign_out user
  #   get root_path
  #   expect(response).not_to render_template(:index) # add gem 'rails-controller-testing' to your Gemfile first.
  # end

  # let!(:user) { FactoryBot.create(:user, :valid_user) }
end
